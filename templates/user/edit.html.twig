{% extends 'base.html.twig' %}

{% block title %}Edit User -
	{{ parent() }}
{% endblock %}

{% block body %}
	<h1>Edit User</h1>

	{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('user_index') }}" class="btn btn-info btn-sm">back to list</a>

	{{ include('user/_delete_form.html.twig') }}
{% endblock %}
